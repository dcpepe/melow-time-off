DATABASE_URL="postgresql://postgres:postgres@localhost:5432/melow_time_off?sslmode=disable"
JWT_SECRET="change-this-to-a-random-secret"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@melow.ai"
SLACK_WEBHOOK_URL=""
# Public base URL used for links in Slack/email notifications.
# Set this to the deployed URL in production (e.g. https://timeoff.melow.ai).
# If unset, links fall back to the host of the incoming request.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
